# LIB
pip freeze > requirements.txt

# VIRTUAL ENV
venv/Scripts/activate
pip install -r requirements.txt

# DOCKER MANUAL
docker build -t pandora-backend .
docker run -p 5000:5000 -d pandora-backend

# DOCKER COMPOSE
docker compose up

# DOCKER PUBLISH
docker tag pandora-backend-docker @username/pandora-backend
docker push @username/pandora-backend

#SONAR QUBE
sonar-scanner
